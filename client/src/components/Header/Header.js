import React from "react";
import { Nav } from "..";
import "./Header.css";

const Header = () => {
  return (
    <header className="main-header">
      <Nav />
    </header>
  );
};

export default Header;
